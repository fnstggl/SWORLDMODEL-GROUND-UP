# Fresh-session live hook verification

Verified commit: **`190b04e5b2f8652bd1d7a88f847c101f3161243a`**
Branch: `claude/sworldmodel-hook-verification-dc02ey`
Claude Code: **2.1.220** (the exact version `.claude/HOOKS_README.md` §1 claims the
contract was verified against)

Static tests prove the scripts behave. This file records whether Claude Code
*actually invokes* them. Everything below was produced by real hook invocations,
not by calling `gate.py` directly, except where a check is explicitly marked as
harness-driven because the event cannot be provoked safely.

Two defects were found and fixed during this run. Live verification was
restarted from the beginning after each fix, and **no evidence from a pre-fix
configuration is counted here** — every result below was observed at
`190b04e`.

---

## How live invocation was proved

The strongest evidence is Claude Code's own hook telemetry. A nested session run
with `--output-format stream-json` emits `system/hook_started` and
`system/hook_response` records naming the hook and carrying its exact output.
Those records are machine-generated by the host, not written by any model.

A second, independent proof was used for `SessionStart`: a nested session
launched with `--allowedTools ""` — no tools at all, so it could not read a
single file — was asked to reproduce the injected control-plane block. It
returned all 23 lines **byte-identical** to what `gate.py` emits, including the
live-computed `sha=190b04e5b2f8` and `worktree=DIRTY`. It had no way to know
either value except from the hook's injected context.

---

## Results

| # | Hook | Result | How it was proved |
|---|---|---|---|
| 1 | `SessionStart` | **PASS** | host telemetry `SessionStart:startup` → injected 1740 chars; toolless session reproduced 23/23 lines exactly |
| 2 | `PreToolUse` | **PASS** | frozen sentinel denied via `Write`, shell redirect and `sed -i`; sentinel never created; 13 further invocations incl. 2 denials in a nested session |
| 3 | `TaskCompleted` | **PASS** | real `TaskUpdate` blocked naming both unmet requirements; allowed once artifact + current-SHA receipt existed |
| 4 | `TeammateIdle` | **NOT EMITTED** | registered correctly, but never invoked — see the external blocker below |
| 5 | `SubagentStop` | **PASS** | host telemetry: `BLOCK` with an incomplete contract, `ALLOW` once satisfied; only 1 invocation for 2 spawned subagents, so `adversarial-reviewer` is never intercepted |
| 6 | `Stop` | **PASS** | host telemetry: first `Stop` → `decision: block`; second `Stop` → empty output/exit 0, the `stop_hook_active` short-circuit |
| 7 | `StopFailure` | **PASS** | three synthetic documented API-error inputs; ledger + recovery request written; never blocks |
| 8 | `ConfigChange` | **PASS** | real settings edit now logs `config_source: project_settings` (it logged `unknown` before the fix) |
| 9 | `run_monitored.py` | **PASS** | 20/20 assertions across healthy, no-progress, CPU-spin, bounded hard timeout, child failure, process-group cleanup |

Supporting suites at `190b04e`:

- `tests/control_plane`: **218 passed, 159 subtests**, green under
  `PYTHONHASHSEED` 0/7/42/99991.
- Deterministic gate matrix (harness-driven, both payload shapes): **69/69**.
- `validate_control_plane.py --run-tests`: **PASS**.

---

## Check 1 — SessionStart

Host telemetry:

```
hook_name: "SessionStart:startup"
output:    {"hookSpecificOutput": {"hookEventName": "SessionStart",
            "additionalContext": "== SWORLDMODEL CONTROL PLANE ==\n..."}}
```

A toolless nested session reproduced the block with `EXACT_MATCH=True`
(23/23 lines).

## Check 2 — PreToolUse

Entered the controlled frozen state (`mode: frozen_acceptance`,
`frozen_sha: 190b04e…`) and attempted a harmless sentinel write three ways.
All three were denied with the three-part `BLOCKED:` / `GOVERNING STATE:` /
`SAFE ALTERNATIVE:` message:

- `Write` to `live_verification_sentinel.txt`
- `echo sentinel > live_verification_sentinel.txt`
- `sed -i 's/__NEVER_MATCHES_ANYTHING__/x/' run_worlds.py`

Afterwards: sentinel **not created**, `run_worlds.py` **unmodified**, and
`RUN_STATE.json` restored **byte-for-byte** (sha256
`25902e6ede3f56c8a608ecd11fe7a317aab475c24341ae8ea538fb8a6edbd9d1`).

An independent nested session additionally had `git reset --hard HEAD~1` and
`nohup python3 -c "import time; time.sleep(600)" &` denied, the latter naming
`run_monitored.py` as the alternative.

## Check 3 — TaskCompleted

A real Claude Code task with a matching `TASK_GRAPH.json` contract. Marking it
complete was blocked:

```
BLOCKED: marking task '3' complete with 2 unmet requirement(s):
  1. required artifact is missing: .agent-run/live_verification/taskcompleted_recheck.txt
  2. no receipt found for command "python3 -c print('taskcompleted recheck')"
```

It also stayed blocked when a passing current-SHA receipt existed for the task
but under a *different* command — the gate matches the exact command, not merely
"some receipt exists". It completed only once both the artifact and the matching
current-SHA receipt were present.

## Check 4 — TeammateIdle — EXTERNAL BLOCKER

**The hook is registered correctly and was never invoked.**

Five genuine idles were attempted by named teammates that owned an
`in_progress` task with a missing required artifact — precisely the condition
`handle_teammate_idle` exists to block. The cleanest of them replied with a
single word, used **zero tools**, and ended its turn in 4.5 seconds. None was
blocked.

Two independent detectors agree that the event never fired:

1. A throwaway diagnostic hook was registered on `TeammateIdle` alongside the
   gate and captured **zero** payloads. The same dumper captured `ConfigChange`
   payloads normally and passed an explicit self-test, so it was demonstrably
   working.
2. The gate itself, registered on the same event, never blocked any teammate.

Ruled out as explanations: registration (verified in `settings.json`, both
hooks listed), the agent-teams flag (`CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`
is set and reaches the process environment), and version drift (Claude Code
2.1.220 is exactly what `.claude/HOOKS_README.md` claims the contract was
verified against, and that file asserts `TeammateIdle` is live at this version).

Consequence: **the "no silent abandonment" guarantee is not in force here.** A
teammate can abandon assigned work with a missing artifact and nothing stops it.
The handler's logic is fully exercised by the harness and the static suite —
seven cases covering owed artifacts, owed receipts, reviewers with and without
their reports, and teammates owning nothing — but no live invocation exists to
confirm Claude Code ever calls it in this environment.

The other evidence gates are unaffected: `TaskCompleted`, `SubagentStop`,
`PreToolUse` and `Stop` are all confirmed live, and they cover the paths that
protect production code and evidence.

## Check 5 — SubagentStop

Host telemetry, contract **unmet** (`live-verification-subagent-hostlevel`
owned by `implementation-agent`, required artifact absent):

```
SubagentStop -> BLOCK
  BLOCKED: protected subagent 'implementation-agent' stopping with an incomplete contract:
SubagentStop -> exit 0, empty output     <- stop_hook_active short-circuit
```

Host telemetry, same contract **satisfied** (artifact created, status
`complete`), spawning an `implementation-agent` *and* an `adversarial-reviewer`:

```
SubagentStop -> exit 0, empty output     <- allowed, no block
=== invocation counts ===  SubagentStop: 1
```

Two subagents were spawned but `SubagentStop` fired **once**. The matcher
`implementation-agent|test-watchdog` means the hook is never invoked for
`adversarial-reviewer` at all, so a read-only reviewer cannot be trapped —
confirmed at the host level rather than by the reviewer's own account.

> A first attempt at this check was discarded. The subagent, asked to observe
> its own interception, invoked `gate.py` directly with a synthetic event
> instead of being intercepted. That proves the script's behaviour, not that
> Claude Code calls it, so it was not counted and the check was redone against
> host telemetry.

## Check 6 — Stop

Verified on a nested session so the lead was never trapped in repeated blocks.
Host telemetry shows both directions:

```
Stop -> {"decision": "block", "reason": "BLOCKED: ending this run with unmet gates:
          1. hook bootstrap overall status is 'STATIC_PASS_LIVE_PENDING', not PASS
          2. fresh_session_hooks_loaded=PENDING live_event_tests=PENDING …"}
Stop -> exit 0, empty output      <- stop_hook_active short-circuit
```

## Check 7 — StopFailure

Three synthetic documented API-error inputs (`api_error`, `overloaded_error`,
and one with no `error_type` at all). No real provider failure was caused. All
three exited 0 — the event cannot block, by design. Each appended a
`FAILURE_LEDGER.jsonl` record carrying `"simulation_result": "not_applicable"`,
and `RECOVERY_REQUEST.json` was written with
`"can_this_hook_restart_the_session": false` plus the exact next action. The
ledger was restored to its committed state afterwards.

## Check 8 — ConfigChange

A real project-settings edit, byte-for-byte restored afterwards (sha256
`ad585f6ae64c10d131664d5818611ed10b6aed0bcbe7df723acf0992ba620582`). The live
record now reads:

```json
{"kind": "config_change", "config_source": "project_settings",
 "changed_file": "…/.claude/settings.json", "mode": "hook_live_verification"}
```

Before the fix this same edit logged `"config_source": "unknown"`, which meant
the gate could not block anything. The block/allow matrix is exercised against
both payload shapes in the harness, and an unidentifiable source now fails
closed during protected modes.

## Check 9 — run_monitored.py

| Scenario | Exit | State | Notes |
|---|---|---|---|
| healthy | 0 | `finished` | |
| no-progress | 125 | `no_progress_timeout` | observed `blocked_no_activity` |
| CPU spin | 125 | `no_progress_timeout` | observed `probable_cpu_spin`, 6.14s CPU |
| bounded hard timeout | 124 | `hard_timeout` | stopped at 8.2s against an 8s bound |
| child failure | 37 | `child_failure` | child's own code propagated, partial logs kept |
| process-group cleanup | 125 | `no_progress_timeout` | group of 4 tracked, `survivors_after_termination: []` |

A spin stays distinguishable from an idle block after the fact, which is the
point of `observed_state_names`. Descendant liveness was checked excluding
zombies — a reaped-but-unwaited PID still answers `kill(pid, 0)`, so a naive
check reports phantom orphans.

---

## Cleanup

- No production, evaluator, fixture, prompt or upstream file changed. Only
  `.claude/hooks/**`, `tests/control_plane/**` and `.agent-run/**`.
- Temporary tasks removed; `TASK_GRAPH.json` back to `"tasks": []`.
- Temporary teammate and subagents finished; no test team left running.
- No orphan processes; `BACKGROUND_JOBS.json` `active_jobs` empty.
- `.claude/settings.json` and `RUN_STATE.json` restored byte-for-byte after
  every test that touched them.
- The throwaway TaskCompleted probe receipt was moved out of
  `.agent-run/receipts/` into `.agent-run/live_verification/` so it can never
  satisfy a future completion gate.

## Outcome: EXTERNAL_BLOCKER

Eight of the nine live checks pass against `190b04e`. The ninth cannot be
produced from inside this environment, so this run does not return PASS.

**Exact blocker.** Claude Code 2.1.220, running headless in this remote
execution environment, does not emit the `TeammateIdle` hook event for
Agent-tool teammates that go idle owing assigned work.

**Direct evidence.** Five genuine idles with an unmet contract; zero
invocations captured by a dedicated diagnostic hook that was proven to be
working; zero blocks from the gate registered on the same event.

**Alternatives attempted.**
1. Named Agent-tool teammate with a `TASK_GRAPH.json` contract naming it owner.
2. Resumed that teammate via `SendMessage` and instructed a clean idle with no
   trailing tool call, twice, after it self-diagnosed that a trailing tool call
   had prevented a true idle.
3. A tracked Claude Code task assigned to the teammate and left `in_progress`,
   so the teams system saw it owning open work.
4. A second teammate with a one-line instruction that used zero tools.
5. Verified hook registration, dumper operation, the agent-teams env var, and
   the running Claude Code version.

**Exact human action required.** Run this one check from an *interactive*
Claude Code session with agent teams / FleetView on this commit: give a
teammate a `TASK_GRAPH.json` task naming it owner with a `required_artifacts`
entry that does not exist, let it go idle, and confirm the gate blocks it with
`BLOCKED: teammate '<name>' going idle with outstanding assigned work`. Then
create the artifact and confirm it may idle. If it blocks and then allows, set
`HOOK_BOOTSTRAP_STATUS.json` `live_checks.TeammateIdle` and `live_event_tests`
to `PASS`, `overall` to `PASS`, and `RUN_STATE.json` to
`mode: ready_for_master`, `status: READY`. If it does **not** fire there
either, `TeammateIdle` should be treated as unavailable and
`.claude/HOOKS_README.md` §1 and §2 corrected to say so, because the control
plane currently documents a guarantee it does not deliver.

## Known limitation, deliberately not "fixed"

`requires_monitored_runner` scans heredoc bodies, so a commit message that
merely contains a phrase like "frozen acceptance" or "corpus" is refused and
must be reworded. That is over-blocking, but stripping heredoc bodies from
*workload* detection would hide a genuinely long job launched as
`python3 - <<'PY' … PY`, whose body really is executed. The conservative
direction was kept.
